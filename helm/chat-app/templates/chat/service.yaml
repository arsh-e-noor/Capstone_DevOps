apiVersion: v1
kind: Service

metadata:
  name: chat-service
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app: chat-service

  ports:
    - port: 8010
      targetPort: 8010